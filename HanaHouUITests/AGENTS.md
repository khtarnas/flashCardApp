# AGENTS.md — HanaHouUITests/

UI tests for HanaHou using XCTest UI testing.

## Current contents

| File | Purpose |
|------|---------|
| `HanaHouUITests.swift` | Default UI test file (Xcode template — to be replaced) |
| `HanaHouUITestsLaunchTests.swift` | Launch performance test (Xcode template) |

## Directives

- UI tests cover user-facing flows (e.g., creating a deck, studying cards).
- Use accessibility identifiers for reliable element targeting.
- Keep UI tests focused on critical paths — unit tests handle logic.
- When adding subdirectories, create an `AGENTS.md` in each.
